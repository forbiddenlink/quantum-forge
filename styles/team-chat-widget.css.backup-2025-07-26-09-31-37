/* Team Chat Widget - Modern Design */
.team-chat-widget {
    position: fixed;
    bottom: var(--space-4);
    right: calc(var(--space-4) + 70px); /* Offset to avoid help button overlap */
    z-index: 9999;
    font-family: 'Inter', sans-serif;
    transition: all var(--duration-normal) var(--ease-spring);
}

/* Chat Toggle Button */
.chat-toggle-btn {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    background: linear-gradient(135deg, var(--primary-500), var(--primary-600));
    color: white;
    border: none;
    border-radius: var(--radius-2xl);
    cursor: pointer;
    box-shadow: var(--shadow-xl);
    transition: all var(--duration-normal) var(--ease-spring);
    position: relative;
    overflow: hidden;
    min-height: 56px;
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.chat-toggle-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.2));
    opacity: 0;
    transition: opacity var(--duration-normal) var(--ease-in-out);
}

.chat-toggle-btn:hover::before {
    opacity: 1;
}

.chat-toggle-btn:hover {
    transform: translateY(-4px) scale(1.05);
    box-shadow: 0 20px 40px rgba(99, 102, 241, 0.4);
}

.chat-icon {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}

.chat-icon svg {
    width: 24px;
    height: 24px;
    transition: transform var(--duration-normal) var(--ease-spring);
}

.chat-toggle-btn:hover .chat-icon svg {
    transform: scale(1.1);
}

.chat-label {
    font-weight: 600;
    font-size: var(--font-size-sm);
    white-space: nowrap;
}

.unread-indicator {
    position: absolute;
    top: -8px;
    right: -8px;
    min-width: 20px;
    height: 20px;
    background: var(--error-500);
    color: white;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-xs);
    font-weight: 700;
    border: 2px solid white;
    animation: pulse-notification 2s infinite;
}

@keyframes pulse-notification {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

/* Chat Panel */
.chat-panel {
    position: absolute;
    bottom: 70px;
    right: 0;
    width: 380px;
    height: 500px;
    background: var(--bg-elevated);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-2xl);
    border: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px) scale(0.95);
    transition: all var(--duration-normal) var(--ease-spring);
    backdrop-filter: blur(20px);
    overflow: hidden;
}

.team-chat-widget.open .chat-panel {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
}

.team-chat-widget.minimized .chat-panel {
    height: 60px;
    overflow: hidden;
}

/* Chat Header */
.chat-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-4);
    background: linear-gradient(135deg, var(--bg-elevated) 0%, var(--bg-secondary) 100%);
    border-bottom: 1px solid var(--border-color);
    position: relative;
}

.chat-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--primary-500), var(--success-500));
    border-radius: var(--radius-2xl) var(--radius-2xl) 0 0;
}

.chat-header-content h3 {
    margin: 0;
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--text-primary);
}

.online-count {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--font-size-xs);
    color: var(--text-secondary);
    margin-top: var(--space-1);
}

.online-indicator {
    width: 8px;
    height: 8px;
    background: var(--success-500);
    border-radius: var(--radius-full);
    animation: pulse-online 2s infinite;
}

@keyframes pulse-online {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}

.chat-header-actions {
    display: flex;
    gap: var(--space-2);
}

.chat-action-btn,
.chat-close-btn {
    width: 32px;
    height: 32px;
    border: none;
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    color: var(--text-secondary);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--duration-normal) var(--ease-in-out);
}

.chat-action-btn:hover,
.chat-close-btn:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
    transform: scale(1.1);
}

.chat-action-btn svg,
.chat-close-btn svg {
    width: 16px;
    height: 16px;
}

/* Online Users Section */
.online-users-section {
    padding: var(--space-3) var(--space-4);
    border-bottom: 1px solid var(--border-color);
    background: var(--bg-secondary);
}

.online-users-scroll {
    display: flex;
    gap: var(--space-2);
    overflow-x: auto;
    padding-bottom: var(--space-2);
    scrollbar-width: none;
    -ms-overflow-style: none;
}

.online-users-scroll::-webkit-scrollbar {
    display: none;
}

.online-user {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-1);
    min-width: 60px;
    cursor: pointer;
    padding: var(--space-2);
    border-radius: var(--radius-lg);
    transition: all var(--duration-normal) var(--ease-in-out);
}

.online-user:hover {
    background: var(--bg-hover);
    transform: translateY(-2px);
}

.user-avatar {
    position: relative;
    width: 36px;
    height: 36px;
    border-radius: var(--radius-full);
    background: var(--primary-100);
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid white;
    box-shadow: var(--shadow-sm);
}

.avatar-emoji {
    font-size: var(--font-size-lg);
}

.status-dot {
    position: absolute;
    bottom: -2px;
    right: -2px;
    width: 12px;
    height: 12px;
    border-radius: var(--radius-full);
    border: 2px solid white;
}

.status-dot.online {
    background: var(--success-500);
}

.status-dot.away {
    background: var(--warning-500);
}

.status-dot.busy {
    background: var(--error-500);
}

.user-name {
    font-size: var(--font-size-xs);
    color: var(--text-secondary);
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 60px;
}

/* Messages Area */
.chat-messages {
    flex: 1;
    padding: var(--space-4);
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    scrollbar-width: thin;
    scrollbar-color: var(--border-color) transparent;
}

.chat-messages::-webkit-scrollbar {
    width: 6px;
}

.chat-messages::-webkit-scrollbar-track {
    background: transparent;
}

.chat-messages::-webkit-scrollbar-thumb {
    background: var(--border-color);
    border-radius: var(--radius-full);
}

.chat-message {
    display: flex;
    gap: var(--space-3);
    animation: slideInUp 0.3s ease-out;
}

/* messageSlideIn animation removed - using slideInUp from animations.css to avoid duplication */

.chat-message.own-message {
    flex-direction: row-reverse;
}

.chat-message.own-message .message-content {
    background: linear-gradient(135deg, var(--primary-500), var(--primary-600));
    color: white;
    border-radius: var(--radius-xl) var(--radius-xl) var(--radius-sm) var(--radius-xl);
}

.message-avatar {
    width: 32px;
    height: 32px;
    border-radius: var(--radius-full);
    background: var(--primary-100);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    border: 2px solid var(--bg-elevated);
    box-shadow: var(--shadow-sm);
}

.message-content {
    flex: 1;
    background: var(--bg-secondary);
    padding: var(--space-3);
    border-radius: var(--radius-xl) var(--radius-xl) var(--radius-xl) var(--radius-sm);
    max-width: 70%;
    position: relative;
}

.message-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-1);
}

.message-author {
    font-size: var(--font-size-xs);
    font-weight: 600;
    color: var(--text-secondary);
}

.chat-message.own-message .message-author {
    color: rgba(255, 255, 255, 0.8);
}

.message-time {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
}

.chat-message.own-message .message-time {
    color: rgba(255, 255, 255, 0.6);
}

.message-text {
    font-size: var(--font-size-sm);
    line-height: 1.4;
    color: var(--text-primary);
    word-wrap: break-word;
}

.chat-message.own-message .message-text {
    color: white;
}

/* Typing Indicator */
.typing-indicator {
    padding: var(--space-3) var(--space-4);
    display: flex;
    align-items: center;
    gap: var(--space-3);
    background: var(--bg-secondary);
    border-top: 1px solid var(--border-color);
    transition: all var(--duration-normal) var(--ease-in-out);
    min-height: 50px;
}

.typing-indicator.hidden {
    opacity: 0;
    transform: translateY(-10px);
    min-height: 0;
    padding: 0 var(--space-4);
}

.typing-dots {
    display: flex;
    gap: 4px;
}

.typing-dots span {
    width: 6px;
    height: 6px;
    background: var(--text-muted);
    border-radius: var(--radius-full);
    animation: typingDot 1.4s infinite ease-in-out;
}

.typing-dots span:nth-child(1) { animation-delay: -0.32s; }
.typing-dots span:nth-child(2) { animation-delay: -0.16s; }
.typing-dots span:nth-child(3) { animation-delay: 0s; }

@keyframes typingDot {
    0%, 80%, 100% {
        transform: scale(0.8);
        opacity: 0.5;
    }
    40% {
        transform: scale(1);
        opacity: 1;
    }
}

.typing-text {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
    font-style: italic;
}

/* Chat Input */
.chat-input-container {
    padding: var(--space-4);
    background: var(--bg-elevated);
    border-top: 1px solid var(--border-color);
}

.chat-input-wrapper {
    display: flex;
    gap: var(--space-2);
    align-items: center;
    background: var(--bg-secondary);
    border-radius: var(--radius-xl);
    padding: var(--space-2);
    border: 1px solid var(--border-color);
    transition: all var(--duration-normal) var(--ease-in-out);
}

.chat-input-wrapper:focus-within {
    border-color: var(--primary-500);
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
}

.chat-input {
    flex: 1;
    border: none;
    background: transparent;
    padding: var(--space-2);
    font-size: var(--font-size-sm);
    color: var(--text-primary);
    outline: none;
    resize: none;
}

.chat-input::placeholder {
    color: var(--text-muted);
}

.chat-send-btn {
    width: 36px;
    height: 36px;
    border: none;
    background: var(--primary-500);
    color: white;
    border-radius: var(--radius-full);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--duration-normal) var(--ease-spring);
    opacity: 1;
}

.chat-send-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    background: var(--bg-muted);
    color: var(--text-muted);
}

.chat-send-btn:not(:disabled):hover {
    background: var(--primary-600);
    transform: scale(1.1);
}

.chat-send-btn svg {
    width: 16px;
    height: 16px;
}

.chat-input-actions {
    display: flex;
    gap: var(--space-2);
    margin-top: var(--space-2);
    justify-content: flex-end;
}

.input-action-btn {
    width: 32px;
    height: 32px;
    border: none;
    background: var(--bg-secondary);
    color: var(--text-secondary);
    border-radius: var(--radius-lg);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--duration-normal) var(--ease-in-out);
}

.input-action-btn:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
    transform: scale(1.1);
}

.input-action-btn svg {
    width: 16px;
    height: 16px;
}

/* Better positioning for desktop to avoid help button */
@media (min-width: 1025px) {
    .team-chat-widget {
        right: var(--space-4); /* Reset to edge on desktop */
        bottom: calc(var(--space-4) + 80px); /* Stack vertically above help button */
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .team-chat-widget {
        bottom: var(--space-3);
        right: var(--space-3); /* On mobile, move back to edge since help button might be in hamburger menu */
    }
    
    .chat-panel {
        width: calc(100vw - var(--space-6));
        max-width: 320px;
        height: 450px;
    }
    
    .chat-toggle-btn {
        padding: var(--space-3);
        min-height: 48px;
    }
    
    .chat-label {
        display: none;
    }
    
    .online-users-section {
        padding: var(--space-2) var(--space-3);
    }
    
    .online-user {
        min-width: 50px;
    }
    
    .user-avatar {
        width: 32px;
        height: 32px;
    }
}

@media (max-width: 480px) {
    .chat-panel {
        position: fixed;
        bottom: 0;
        right: 0;
        left: 0;
        width: 100%;
        height: 60vh;
        border-radius: var(--radius-2xl) var(--radius-2xl) 0 0;
        max-width: none;
    }
    
    .team-chat-widget.open .chat-panel {
        transform: translateY(0) scale(1);
    }
}

/* Dark mode adjustments */
[data-theme="dark"] .chat-panel {
    background: var(--bg-elevated);
    border-color: var(--border-color);
}

[data-theme="dark"] .chat-header {
    background: linear-gradient(135deg, var(--bg-elevated) 0%, var(--bg-secondary) 100%);
}

[data-theme="dark"] .message-content {
    background: var(--bg-secondary);
}

[data-theme="dark"] .chat-input-wrapper {
    background: var(--bg-secondary);
    border-color: var(--border-color);
} 