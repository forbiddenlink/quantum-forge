/* Task System Component Styles
 * This file contains all styles related to the task system, consolidated from various files
 * to eliminate duplication and improve maintainability.
 * Task system variables now consolidated in critical-fixes.css
 */

/* Task Card */
.task-card {
  background: var(--task-card-bg);
  border: 1px solid var(--task-card-border);
  border-radius: var(--radius-xl);
  padding: var(--space-4);
  position: relative;
  transition: all var(--duration-normal) var(--ease-spring);
  overflow: hidden;
}

.task-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: var(--task-card-accent-width);
  height: 100%;
  background: var(--task-priority-low);
  opacity: 0;
  transition: opacity var(--duration-normal) var(--ease-in-out);
}

.task-card:hover {
  transform: translateY(-4px);
  border-color: var(--task-card-hover-border);
  box-shadow: var(--task-card-shadow);
}

.task-card:hover::before {
  opacity: 1;
}

/* Priority Accents */
.task-card.priority-high::before {
  background: var(--task-priority-high);
}

.task-card.priority-medium::before {
  background: var(--task-priority-medium);
}

.task-card.priority-low::before {
  background: var(--task-priority-low);
}

/* Task Card Header */
.task-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-4);
}

.task-title {
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
}

.priority-icon {
  font-size: var(--font-size-sm);
}

/* Task Card Content */
.task-card-content {
  margin-bottom: var(--space-4);
  color: var(--text-secondary);
  line-height: 1.5;
}

/* Task Progress */
.task-card-progress {
  margin-bottom: var(--space-4);
}

.progress-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-2);
}

.progress-text {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
  font-weight: 500;
}

.progress-bar {
  width: 100%;
  height: var(--task-progress-height);
  background: var(--task-progress-bg);
  border-radius: var(--radius-full);
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: var(--task-progress-fill);
  border-radius: var(--radius-full);
  transition: width var(--duration-normal) var(--ease-in-out);
  position: relative;
}

.progress-fill::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, transparent 0%, rgba(255, 255, 255, 0.1) 50%, transparent 100%);
}

/* Task Meta Information */
.task-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.task-meta-items {
  display: flex;
  gap: var(--space-3);
  align-items: center;
}

.task-assignee {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.task-due-date {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
}

/* Task Actions */
.task-actions {
  display: flex;
  gap: var(--space-2);
}

.task-action-btn {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-lg);
  font-size: var(--font-size-sm);
  font-weight: 500;
  border: none;
  cursor: pointer;
  transition: all var(--duration-normal) var(--ease-spring);
}

.task-action-btn.primary {
  background: var(--gradient-primary);
  color: white;
}

.task-action-btn.secondary {
  background: var(--bg-secondary);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
}

.task-action-btn:hover {
  transform: translateY(-1px);
  box-shadow: var(--shadow-md);
}

.task-action-btn svg {
  width: 16px;
  height: 16px;
}

/* Task Grid Layout */
.task-cards-grid {
  display: grid;
  gap: var(--space-4);
}

@media (min-width: 640px) {
  .task-cards-grid {
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  }
}

/* Task Analytics */
.analytics-grid {
  display: grid;
  gap: var(--space-4);
}

@media (min-width: 640px) {
  .analytics-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .analytics-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (min-width: 1280px) {
  .analytics-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* Task Stats - Using master definitions from components.css */

/* Dark Theme Overrides */
[data-theme="dark"] .task-card {
  background: var(--bg-elevated);
}

[data-theme="dark"] .progress-fill::after {
  background: linear-gradient(90deg, transparent 0%, rgba(255, 255, 255, 0.05) 50%, transparent 100%);
}